# Enquiries — Implementation Plan

**Spec:** `docs/superpowers/specs/2026-09-09-enquiries-design.md`. Tests first for everything in `domain/`. `pnpm verify` **and** `pnpm test:db` before every push.

**Global constraints:** public repo (no identity, no real data); console English only; no business logic in components, routes or SQL beyond the throttle (access control) and the tenant resolution; migrations in the trunk's `900–949` half with `-- Needs:` and `-- rollback:`; the round is declared in `docs/CHANGE-REQUESTS/trunk-notes.md`.

- [x] **T1 domain/enquiry** — `parse.ts` (`parseEnquiry(form)`: honeypot, clamps, E.164 with the old function's three rules, name+number required, tri-state consent, source); `lead.ts` (`leadFromEnquiry(enquiry)` → the `CreateClientBody` shape; name split first token / rest); tests for every branch; barrel; browser-safe.
- [x] **T2 migration 916 + policies** — `enquiry` table (standard columns bar `created_by`), `app.lodge_enquiry(jsonb)` definer (tenant resolve, throttle 5/10min by `ip_hash`, insert, revoke/grant), `db/policies/enquiry/readers.sql` + `writers.sql` (select/update for owner, admin, lead_practitioner; no insert), the exemption line in `.claude/rules/data-model.md`, `docs/SPEC/00-data-model.md` entry. `tests/db/enquiries.test.ts`: lodge works for the practice; throttle refuses the sixth; `app_role` cannot insert directly; readers see rows, a practitioner does not.
- [x] **T3 the door** — `app/api/enquiries/door.ts` ahead of the fence with `rateLimit` by address; form or JSON; CORS for the two origins (`ENQUIRY_ORIGINS` override); OPTIONS 204; honeypot and throttle answer 200 `{ok:true}`; missing name/number 400; a `jsonOnly` exemption for this one route. Tests: `tests/db/enquiries.test.ts` extends with door cases.
- [x] **T4 actioning routes** — `GET /api/enquiries` (roles; `logReads` entity `enquiry`), `POST /api/enquiries/:id/convert` (reuses the lead creation path: `app.next_mrn`, client `lead`, contact `self`; sets status/client_id/actioned_*; scrubs), `POST /api/enquiries/:id/dismiss` (reason; scrubs). Schema in `schema.ts`. Tests: convert produces an audited client and a scrubbed row; dismiss scrubs; a practitioner is refused.
- [x] **T5 the screen** — `app/admin/enquiries/EnquiriesPage.tsx` on the `Table` component; route and rail entry gated by `canOpenEnquiries` in `app/shell/routing.ts`; page test.
- [ ] **T6 round note + gates** — trunk-notes round 38; `pnpm verify`; `pnpm test:db`; reviews (security, compliance, schema) as tonight; PR.
- [ ] **T7 website** — `ENDPOINT` in the 22 mirrored pages → `https://app.mcwellnessuae.com/api/enquiries`; validate; redeploy the static site; verify by content on both languages.
- [ ] **T8 production** — merge on green; 916 with its ledger row; rebuild the app (migrations first); lodge one enquiry from the live site's form path with curl, see it in the screen, convert it, erase the lead; report; the old project is then safe to pause — operator's call.
