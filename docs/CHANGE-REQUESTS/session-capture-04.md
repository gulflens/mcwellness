# session-capture-04 — the shared-zone changes piece eight needs

**Status.** Authorised by the integrator on 2026-09-05 and, under cost rule 6 of `docs/HANDOVER.md`, applied in the piece's own pull request rather than a separate trunk round, as `client-portal-05.md` was for piece seven. The builder edits the paths below for this piece only and lists each edit in the pull-request body. Sections 1 (installability), 2 (the photo's bytes) and 5 (sign-out) of `session-capture-02.md` are met by this file, in the form `docs/SPEC/practitioner-phone.md` decides.

**What, and why.**

1. `package.json` and the lockfile: `vite-plugin-pwa` as a development dependency; `vite.shared.ts` gains the plugin in `injectManifest` mode with `app/shell/sw.ts` as the source and development disabled (spec section 3.2, decision 1). `pnpm audit:deps` covers it from the next weekly run.
2. `app/shell/sw.ts` (new), `app/shell/sw.test.ts` (new), `public/manifest.webmanifest` (new), `public/icon.svg` (new), `index.html` (the manifest link and the touch icon), `app/shell/main.tsx` (the registration after first render, production only): spec sections 3.1 and 3.2. The worker's rules and its two messages are those of `session-capture-02.md` section 1d, on top of the precache. *Added by the integrator on 2026-09-05, in the fix round.* `app/shell/sw.test.ts` was built and not named: a worker whose whole value is what it never caches is not provable by a walk, so its test sits beside it.
3. `app/shell/auth/AuthContext.tsx`: sign-out calls the outbox's own `forgetDevice()` and posts `forget-reads` to the worker (spec section 3.5; `session-capture-02.md` sections 1f and 5b, unchanged).
4. `app/api/create-api.ts`, `app/api/server.ts` and `app/api/_middleware/request-context.ts`: `PHOTO_LIMIT_BYTES` and the one exemption from the body cap and from `jsonOnly` for `PUT /api/sessions/:id/photo` (spec section 4.3); the routing seam built from the environment by `routingFromEnv` in `app/api/_middleware/routing/`, passed as an option the way `storage` is; `app/api/kit` and `app/api/routing` mounted after the fence. *Added by the integrator on 2026-09-05, in the fix round.* `app/api/server.ts` is where an option built from the environment is actually built and handed to `createApi`, exactly as `storageFromEnv` is, so the seam could not be wired without it; and `app/api/_middleware/request-context.ts` gains the seam's own slot on `ApiEnv` below the fence, without which the option could be passed but no route could ever read `c.get('routing')`.
5. `app/api/serve-app.ts`: `manifest.webmanifest`, `icon.svg` and `sw.js` served from the build's root with their own content types, the worker with `Cache-Control: no-cache`, everything else as today.
6. `domain/shared/routing.ts` (new, with its test): the seam interface, the straight-line arithmetic and the hour bucket (spec sections 5.1 and 5.6). `app/api/_middleware/routing/index.ts`, `google.ts`, `straight-line.ts` and `seam.test.ts` (new): the two implementations, the choice by `ROUTING_PROVIDER`, the refusal to start without an explicit choice outside development, and the forced-fallback test. `.env.example`: `ROUTING_PROVIDER` and `GOOGLE_MAPS_SIGNING_SECRET` beside the existing `GOOGLE_MAPS_API_KEY`, with the comment saying the key is a server key.
7. `domain/shared/actor.ts` (and its test): `kit.manage` (owner, admin, lead practitioner), `kit.read` (the same three, and a practitioner for their own items), `routing.day.read` (a practitioner for their own day). Spec section 6.2.
8. `app/shell/App.tsx`, `app/shell/adminAccess.ts`, `app/shell/components/Rail.tsx`, `app/shell/AdminLayout.tsx`, `app/shell/components/Icons.tsx`: `/admin/kit` behind `canOpenKit` (`kit.manage`), rail entry "Kit". `routing.test.ts` is unchanged. *Added by the integrator on 2026-09-05, in the fix round.* The last two were edited and not named: `AdminLayout.tsx` is where a rail section is admitted or hidden per person, and a rail entry needs a glyph, which lives in `Icons.tsx`.
9. `db/seed/generate.ts`, `apply.ts`, `render.ts` and `tests/db/seed.test.ts`: an amplifier per seeded practitioner, calibrated and in date, and one unassigned amplifier whose calibration is overdue, so the block is demonstrable without blocking the staging demo's own visit. Serial numbers in the reserved shape the README describes for ids; no real model name.
10. `domain/shared/audit-narrative.ts` and `domain/shared/audit-narrative.test.ts`: sentences for a `kit` row and for the action `session.photo_filed`, both languages; `drive_estimate` takes the generic fallback on purpose (spec section 10). *Added by the integrator on 2026-09-05, in the fix round.* The test was edited and not named: every other sentence in that file is asserted in its own test, and a new one is no different.
11. `docs/SEAMS.md`, `docs/COMPLIANCE/approved-vendors.md`, `docs/SPEC/00-data-model.md`: **done by the integrator in the spec's own pull request**, so the builder starts against a register and a model that already describe what it builds.
12. `tests/scheduling/TodayPage.test.tsx`. *Added by the integrator on 2026-09-05, in the fix round.* Today carried one unconditional line, "Today needs a connection", because there was no local store; from this piece the worker keeps the day, so the line becomes a band shown only when the device is actually offline and saying that checking in is the part that needs a signal. The one test in scheduling's file that asserted the old sentence asserts the band instead. `docs/SPEC/OWNERSHIP.md` gives this piece the whole practitioner face including Today; the test of a screen this piece was told to change goes with it, and nothing else in that file moves.
13. `.claude/rules/data-model.md`. *Added by the integrator on 2026-09-05, in the fix round.* `app.setup_photo_filing` (migration 306) joins the named standard-column exemptions beside `app.erasure_active`, with the same clause: a transaction marker, not a business row, carrying only the `txid` that names the transaction. The rule lists the exempt tables by name and says nothing else is exempt, so a new bookkeeping table has to be recorded where the rule lives.
14. `app/api/_middleware/security.ts`, `tests/security/headers.test.ts` and `docs/SECURITY.md`. *Added by the integrator on 2026-09-06, in the second round.* `img-src` widens from `'self' data:` to `'self' data: blob:`, and nothing else in the policy moves. Both of this piece's pictures — the day's map and the last sensor placement — are fetched by the app itself through `apiFetch`, because every route below the fence authenticates on a bearer header and the browser's own image request would carry none, and are then shown from a revocable object URL. A `blob:` URL's scheme is `blob`, which `'self'` cannot match, so the served app refused both; and a `blob:` URL can be created only by the app's own scripts, so admitting the scheme admits no third party. The directive is pinned exactly in `tests/security/headers.test.ts`, spec section 3.6 records the widening, and `docs/SECURITY.md` layer 2 says `data:` and `blob:`.

**Spec.** `docs/SPEC/practitioner-phone.md` sections 3 to 9 and 12.
