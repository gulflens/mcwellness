node_modules/
dist/
.env
.env.local
.claude/worktrees/
.claude/settings.local.json
*.log
.terraform/
*.tfstate*
.DS_Store
coverage/
*.tsbuildinfo

# Design review evidence and browser scratch
.impeccable/review/
.playwright-mcp/

# Vite reads .env.<mode> and .env.*.local; only the example is tracked
.env.*
!.env.example

# Skills are kept once, in .agents/ (every harness reads it) and linked from .claude/.
# The copies these installers make for harnesses this project does not use are ignored.
.github/skills/
.github/agents/
.github/hooks/
.codex/
