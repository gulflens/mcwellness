// Synthetic seed generators arrive with PR 4 (one tenant, three practitioners with
// credentials, twenty clients with contacts, locations and consents), using the
// reserved fake ranges only. Until then this command is a no-op that exits cleanly.
console.log('Seed generators arrive with PR 4. Nothing to do yet.');
